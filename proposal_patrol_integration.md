# パトロール結果からの要因解析連携機能 提案書

## 1. 概要
パトロール結果一覧（`PatrolResult`）で絞り込んだ不具合データに対し、関連する特性値データ（`CharacteristicValue`）を自動的に結合し、要因解析機能（Mona Analytics）へシームレスに連携する機能を提案します。

## 2. 業務フロー (画面遷移)

1.  **パトロール結果一覧 (`/patrol/list`)**
    *   ユーザーは期間、機種、不具合ランク等でデータを絞り込みます。
    *   画面右上に**「この条件で要因解析データを作成」ボタン**を追加します。

2.  **データセット作成設定 (モーダル/ダイアログ)**
    *   ボタン押下時に設定ダイアログを表示します。
    *   **目的変数設定:**
        *   例: 「ランクA/Bを不具合(1)、ランクCを正常(0)とする」等の定義を選択。
    *   **説明変数(特性値)設定:**
        *   紐付ける特性データのカテゴリを選択 (例: 全て, 温度系のみ, etc.)。

3.  **データ生成 & 解析実行**
    *   システムがバックグラウンドでデータを結合し、解析用データセット(DataFrame)を生成します。
    *   生成されたデータを保持した状態で、**要因解析画面 (`/analysis`)** へ遷移します。
    *   （ファイルアップロードを介さず、メモリ上または一時ストレージ経由でデータを渡します）

## 3. 必要な機能開発

### フロントエンド
*   **パトロール一覧画面:** 「解析データ作成」ボタンと設定モーダルの追加。
*   **API連携:** 検索条件と設定を送信し、解析開始をリクエストする処理。

### バックエンド
*   **データ結合ロジック (`services/dataset_builder.py` 新設)**
    *   `PatrolResult` と `CharacteristicValue` を `machine_id` + `date` で結合する処理。
    *   特性値を横持ち（Pivot）に変換する処理（`characteristic_id` を列名に）。
*   **解析実行API拡張:**
    *   既存の `upload` APIに加え、`analyze_from_patrol` (検索条件を受け取る) または `analyze_dataset` (生成済みデータを受け取る) エンドポイントが必要。

## 4. データ構造イメージ

| date | machine_id | rank (Target) | temp_zone1 (Feature) | pressure (Feature) | ... |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2025-01-01 | M001 | B (1) | 210.5 | 5.2 | ... |
| 2025-01-01 | M002 | A (1) | 215.0 | 4.8 | ... |
| 2025-01-01 | M003 | C (0) | 198.2 | 5.0 | ... |

## 5. 課題・検討事項
*   **データ量:** 特性値が大量にある場合、結合処理に時間がかかる可能性があります（非同期処理検討）。
*   **データ欠損:** パトロール結果はあるが、特性値が存在しない日付/機番の扱い（デフォルトは欠損として解析ロジック任せ）。
