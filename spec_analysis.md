# 不具合要因解析ツール「Mona Analytics」仕様書

## 1. 概要
本ツールは、製造・開発データセット（CSV/Excel/Parquet）を入力するだけで、不具合発生フラグ（0/1）と関連の強い変数を自動抽出し、ランキング形式で可視化するWebアプリケーションである。

**最大の特徴は「サンプルの規模に応じた解析ロジックの自動切り替え」にある。**
不具合データがわずか数件のフェーズ（立ち上げ期）から、数千件蓄積されたフェーズ（量産期）まで、統計的妥当性を保ちながら一貫して利用できる解析基盤を提供する。

## 2. 目的
1.  **脱・属人性:** 経験や勘に依存せず、データに基づいた定量的な不具合要因の絞り込みを行う。
2.  **アクションの明確化:** 単なる「関連度」だけでなく、「値をどうすれば不具合が減るか（方向性）」を提示する。
3.  **ハイブリッド解析:** 少数データでは「仮説生成」、大量データでは「高精度モデル」と、フェーズに合わせた最適な解を出す。                                                                              

## 3. 想定ユーザー
* **品質保証・管理部門:** 不具合の傾向管理、再発防止策の策定
* **製造・設計部門:** パラメータ適正値の検討
* **社内データ分析担当:** 初動分析の工数削減
    * ※ 統計・機械学習の深い専門知識は不要なUI設計とする。

## 4. システム構成・実現方式
開発者の技術スタック（Azure/Python）に最適化した構成とする。

* **プラットフォーム:** Webアプリケーション
* **フロントエンド/バックエンド:** **Streamlit** (Python)
    * 理由: 高速な開発サイクル、インタラクティブなデータ操作、Pythonエコシステムの活用。
* **インフラ:** Azure App Service (Container)
* **認証:** Microsoft Entra ID (旧 Azure AD) 連携
* **解析ライブラリ:** `pandas`, `scipy`, `statsmodels`, `lightgbm`, `shap`

## 5. 入力仕様
### 5.1 データセット
* **形式:** CSV, Excel (.xlsx), Parquet
* **構成:** 1行1サンプル（製品ID、製造ロット等）
* **列:**
    * **目的変数:** 不具合発生フラグ（0 = 正常, 1 = 不具合）
    * **説明変数:** 数値型（温度, 圧力等）、カテゴリ型（装置名, 作業者等）

### 5.2 前処理（自動実行）
* **欠損値処理:**
    * 数値: 中央値補完 + 欠損フラグ列の作成
    * カテゴリ: "Unknown" カテゴリとして扱う
* **エンコーディング:** カテゴリ変数はOne-Hot Encoding（多水準の場合は頻度エンコーディング等を検討）
* **スケーリング:** 解析手法に応じて標準化を実施

## 6. 解析ロジック：不具合サンプル数別 自動切替モード

不具合ありデータ（$N_{defect}$）の数に応じて、4つのモードを自動で切り替える。

| モード | 不具合数($N$) | 信頼度ラベル | 採用手法・アルゴリズム | 目的・特徴 |
| :--- | :--- | :--- | :--- | :--- |
| **① 探索モード** | 5 〜 30 | 🟡 仮説 | **Cliff's Delta (効果量)**<br>**Mann-Whitney U検定** | **「見逃し防止」**<br>P値による厳密な足切りはせず、分布の差が大きいものを拾い上げる。外れ値に強いノンパラメトリック手法を採用。 |
| **② 準統計モード** | 30 〜 100 | 🟢 参考 | **点双列相関係数**<br>**Firth補正ロジスティック回帰** | **「バランス」**<br>統計的検出力が出始める段階。分離完全性（不具合率0%の条件など）に対応できるFirth補正を用いる。 |
| **③ 標準モード** | 100 〜 300 | 🔵 高信頼 | **L1正則化ロジスティック回帰**<br>**多重検定補正(BH法)** | **「厳密性」**<br>偽陽性（False Positive）を抑えるため、多重検定の補正や正則化を取り入れ、本当に効いている変数を絞る。 |
| **④ AI解析モード** | 300 〜 | 🔵 高信頼 | **LightGBM + SHAP** | **「非線形・交互作用」**<br>変数間の複雑な組み合わせ（AかつBの時のみ不具合）を検知。SHAP値で寄与度を分解する。 |

## 7. 出力仕様（UIデザイン）

### 7.1 ランキングテーブル（Top 20）
以下の項目を持つテーブルを表示する。

| 表示項目 | 説明 | 視覚表現 |
| :--- | :--- | :--- |
| **順位** | 総合スコア順 | 1位〜20位 |
| **変数名** | 説明変数のカラム名 | テキスト |
| **方向性** | その変数がどうなると不具合が増えるか | ⬆️ (高いとNG)<br>⬇️ (低いとNG)<br>⚠️ (特定値でNG) |
| **スコア** | モードごとの重要度指標 | バー表示 |
| **安定度** | Bootstrap法による再現性評価 | ★★★★★ (5段階) |
| **備考** | 解析上の注意点 | 「欠損多」「分布偏り」等 |

### 7.2 安定度評価（Reliability Check）
* **手法:** データセットからランダムに再抽出（Bootstrap法）を100回行い、その変数がいずれの試行でも上位に入り続けるかを検証する。
* **意義:** 「たまたまそのデータの組み合わせでランクインしただけ」のノイズ変数を排除する。

### 7.3 詳細ドリルダウン（グラフ）
ランキングの行をクリックすると、以下のグラフを展開表示する。
* **分布比較:**
    * 小サンプル時: **ストリッププロット**（個々のデータを点で表示し、外れ値を確認）
    * 大サンプル時: **バイオリンプロット** または **箱ひげ図**
* **不具合率推移:** 数値変数をビン分割し、区間ごとの不具合発生率を折れ線グラフ化。

## 8. 非機能要件
* **レスポンス:** 10万行×1000列程度のデータに対し、3分以内で解析完了すること。
* **セキュリティ:** アップロードされたデータは解析終了後、メモリ上から即座に破棄されること（ストレージに永続化しない）。
* **エラーハンドリング:** 多重共線性（VIF高）や定数項（分散0）の変数は自動除外または警告表示する。

## 9. 将来拡張案
1.  **時系列解析対応:** 日付列を指定し、「特定の期間だけ影響度が高まった変数」を抽出する機能。
2.  **レポート自動生成:** 解析結果とグラフをまとめたPDF/HTMLレポートのダウンロード機能。
3.  **LLMによる考察:** ランキング上位の変数名から、Gemini等のLLMを用いて「物理的に考えられるメカニズム」をサジェストする機能。