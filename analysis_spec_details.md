# Mona Analytics 解析ロジック詳細仕様書

## 1. 概要
本ドキュメントは、不具合要因解析ツール「Mona Analytics」における、自動解析ロジックおよび各モードの詳細仕様を定義するものです。

## 2. 前処理ロジック (共通)
全ての解析モードを実行する前に、以下の前処理が適用されます。

### 2.1 データクリーニング
*   **ターゲット欠損除去:** 目的変数（不具合フラグ）が欠損している行は除外。
*   **欠損値補完:**
    *   数値列: 中央値 (`median`) で補完。
    *   カテゴリ列: 文字列 `"Unknown"` で補完。

### 2.2 ID列・カーディナリティ除外
過学習およびノイズ混入を防ぐため、以下の条件に合致する列は解析対象外（除外）とします。
*   **完全ユニーク:** 全行の値が異なる列。
*   **IDらしき列:** 値のバリエーションが全体の90%以上、かつ列名に `id`, `no`, `code` のいずれか（大文字小文字区別なし）が含まれる列。
*   **定数:** 全ての行で同じ値を持つ列。

### 2.3 エンコーディング
*   カテゴリ変数（文字列型など）は、One-Hot Encoding により数値化（ダミー変数化）されます。

---

## 3. 解析モード仕様

不具合データ数（$N_{defect}$）に応じて、以下の4つのモードから最適なものを自動選択します（手動指定も可能）。

### ① 探索モード (Exploration Mode)
*   **適用条件:** $N_{defect} < 30$
*   **目的:** データ数が極めて少ないフェーズでの「仮説生成」。P値による厳密な棄却は行わず、少しでも差異がある変数を拾い上げる。
*   **採用アルゴリズム:**
    *   **Cliff's Delta (効果量):** ノンパラメトリックな効果量指標。分布の重なり具合を評価する。
        *   スコア範囲: -1.0 〜 1.0 (絶対値が大きいほど影響大)
    *   **Mann-Whitney U検定:** 2群間の代表値の差の検定（参考値としてP値を算出）。
*   **使用ライブラリ:** `scipy.stats`
*   **ランキング順:** Cliff's Delta の絶対値の降順。

### ② 準統計モード (Quasi-Stat Mode)
*   **適用条件:** $30 \le N_{defect} < 100$
*   **目的:** 統計的な傾向が見え始める段階での解析。計算コストが低く、ロバストな相関係数を用いる。
*   **採用アルゴリズム:**
    *   **点双列相関係数 (Point Biserial Correlation):** 連続変数と二値変数の相関係数。
        *   スコア範囲: -1.0 〜 1.0
*   **使用ライブラリ:** `scipy.stats`
*   **ランキング順:** 相関係数の絶対値の降順。

### ③ 標準モード (Standard Mode)
*   **適用条件:** $100 \le N_{defect} < 300$ (または AIモード不可時のフォールバック)
*   **目的:** ある程度のデータ量が確保できた段階での厳密な要因特定。多重共線性の影響を抑え、変数を選択する。
*   **採用アルゴリズム:**
    *   **L1正則化ロジスティック回帰 (Lasso):** 不要な変数の係数を0にする特性を利用して、重要な変数を絞り込む。
*   **使用ライブラリ:** `sklearn.linear_model.LogisticRegression`
*   **ランキング順:** 回帰係数 (Coefficient) の絶対値の降順。

### ④ AI解析モード (AI Analysis Mode)
*   **適用条件:** $N_{defect} \ge 300$ (かつ LightGBM利用可能)
*   **目的:** 大量データからの複雑なパターン（非線形・交互作用）の発見。
*   **採用アルゴリズム:**
    *   **LightGBM (Gradient Boosting Decision Tree):** 高速かつ高精度な勾配ブースティングモデル。
    *   **SHAP (SHapley Additive exPlanations):** モデルの予測に対する各変数の寄与度を算出する。
        *   スコア: SHAP値の絶対値平均 (Global Importance)。
*   **使用ライブラリ:** `lightgbm`, `shap`
*   **システム要件:** macOSの場合 `libomp` がインストールされていること。
*   **ランキング順:** Mean Absolute SHAP Value の降順。

---

## 4. 出力指標定義

### 方向性 (Direction)
各変数が不具合に対して「どうなるとリスクが高まるか」を示します。

| アイコン | 定義 | 判定ロジック |
| :--- | :--- | :--- |
| **⬆ NG** | 値が大きいほど不具合率が高い | 係数 > 0, 相関 > 0, Cliff's d > 0, または SHAP値との相関 > 0 |
| **⬇ NG** | 値が小さいほど不具合率が高い | 係数 < 0, 相関 < 0, Cliff's d < 0, または SHAP値との相関 < 0 |

### 詳細グラフ (Boxplot)
*   ランキング上位の変数について、正常品（0）と不具合品（1）の分布を箱ひげ図で可視化します。
*   データは四分位数（Min, Q1, Median, Q3, Max）を用いて描画されます。
