# Mona 仕様書

## 概要
機器データを可視化するWebアプリケーション。FastAPI + Jinja2 + ECharts で構築。
機器の設計エンジニアが不具合解析および特性値監視が可能なWebアプリを提供。

---

## 技術スタック

| カテゴリ | 技術 |
|---------|------|
| バックエンド | Python FastAPI |
| テンプレートエンジン | Jinja2 |
| グラフ描画 | ECharts + プラグイン（annotation, boxplot） |
| アイコン | Lucide Icons (CDN) |
| スタイリング | Vanilla CSS（ライトモードデザイン） |
| データベース | SQLAlchemy 2.0 + Alembic（SQLite/PostgreSQL/SQL Server） |
| 認証 | Cookieセッション + bcrypt |

---

## 環境設定

`.env` ファイルで環境を設定:

```bash
# アプリケーション設定
APP_ENV=development          # development / staging / production
APP_DEBUG=true
APP_HOST=127.0.0.1
APP_PORT=8000

# データベース設定
DATABASE_URL=sqlite:///./machine_viz.db

# データソース設定
USE_DUMMY_DATA=true          # true=ダミーデータ, false=データベース
# ダミーデータはシード値により決定論的に生成（同一条件で同一データを保証）

# セキュリティ
SECRET_KEY=your-secret-key
LOG_LEVEL=DEBUG
```

---

## 認証・認可

### ログイン
- Cookieベースのセッション認証
- パスワードはbcryptでハッシュ化
- セッション有効期限: 7日間

### 管理者ユーザー作成
```bash
PYTHONPATH=. uv run python scripts/create_admin.py <username> <password>
```

### ページ保護
- 全ページ（ログイン画面を除く）は認証必須
- 未ログイン時はログイン画面にリダイレクト
- ナビゲーションバーにログアウトボタン

---

## 機能

### 1. トップ画面（ダッシュボード）
- **レイアウト**: サマリーカード + 目的別ワークフローに基づいたナビゲーションカード
- **サマリーカード**: 
  - データの最終更新日
  - パトロール結果 直近1週間の警告あり件数
- **ワークフロー**:
  - **不具合監視・分析フロー**: パトロール結果一覧 -> 不具合傾向表示 -> 機番履歴表示
  - **機番調査フロー**: 機番検索 -> 機番履歴表示 -> データカタログ
- **ナビゲーションバー**:
  - ブランドロゴにアイコン（猫）
  - 各メニューにLucide Iconsを使用
  - メニュー: パトロール結果、不具合傾向、機番検索、機番履歴、データカタログ

### 2. 機番検索
#### 2-1. 不具合から検索する
- **検索条件**:
  - 機種シリーズ（単一選択）
  - 不具合発生日（From-To形式で範囲指定）
  - 不具合分類（単一選択）
- **結果表示**:
  - 該当機番一覧をテーブル表示
  - テーブルには機番、機種シリーズ、不具合分類、不具合発生日、製造月、操作ボタンを表示
  - **操作**:
    - 機番リンクまたは「履歴表示」ボタンクリックで機番履歴表示へ遷移
- **UX機能**:
  - 検索中のローディング表示
  - エラー発生時の表示

#### 2-2. 機番属性から検索する
- **検索条件**:
  - 機種シリーズ（単一選択）
  - 機番（部分一致テキスト入力）
  - 製造月（From-To形式で範囲指定）
- **結果表示**: 
  - 該当機番一覧をテーブル表示
  - テーブルには機番、機種シリーズ、不具合分類、不具合発生日、製造月、操作ボタンを表示
  - **操作**:
    - 機番リンクまたは「履歴表示」ボタンクリックで機番履歴表示へ遷移
- **UX機能**:
  - 検索中のローディング表示
  - エラー発生時の表示


### 3. 断面データ表示（EDA型ダッシュボード）
- Phase2以降に検討
- **レイアウト**: 3エリア分割（左：条件設定、中央：可視化、下：詳細データ）
- **基本条件**: 機種シリーズ、機種番号、対象月を選択
- **特性値条件**: データカテゴリー、特性値ID、集計方法を選択
- **チャート種別タブ**:
  - **ヒストグラム**: 値の分布を棒グラフで表示（相対頻度表示対応）
  - **散布図**: 2変数の相関を可視化、相関係数を自動表示
  - **箱ひげ図**: 機種ごとの分布（中央値、四分位範囲）を比較
- **グループ比較**: 比較対象の機番をテキストエリアで入力し、選択機番群とその他機番群で比較可能
- **URL状態管理**: 
  - チャート種別・フィルタ条件をURLパラメータに反映
  - URLアクセス時に自動でグラフ表示
- **詳細データ表示機能**:
  - ヒストグラムのバーをクリックで該当範囲のデータを詳細テーブルに表示
  - 折れ線グラフの点をクリックで該当データを詳細テーブルに表示
  - **ページネーション**: デフォルト20件/ページ、10/20/50/100件から選択可
  - **ページジャンプ**: ページ番号を入力して直接移動
  - **ソート機能**: 機番・値のヘッダークリックで昇順/降順切替
  - **CSVダウンロード**: 詳細データをCSVファイルとしてエクスポート
- **フィルタプリセット保存**: 条件をプリセットとして保存し、ワンクリックで呼び出し可能（LocalStorage）

### 4. 機番履歴表示
- 1機番の履歴を表示
- **レイアウト**: 2段構成のフィルターバー + チャートエリア + 詳細データ
  - **フィルターバー上段（データ選択）**: 系列1・系列2の条件（カテゴリー、特性値、集計）を設定。系列2は追加/削除可能。
  - **フィルターバー下段（表示設定）**: グラフ種別、X軸、アノテーション設定。
- **機番入力**: テキストエリアに機番をカンマまたは改行区切りで入力（1台のみ）
- **データ選択**: 
  - イベント情報（FW更新、部品交換、メンテナンス、不具合発生）
  - 機番属性
  - 特性値
- **イベント情報表示**:
  - イベント情報を表形式で表示
- **機番属性表示**:
  - 機番属性を表形式で表示
- **特性値表示**:
  - 特性値をグラフ表示
  - **グラフ種別切り替え**:
    - 量的変数と質的変数どちらにも対応できるように以下のグラフを切り替え可能
    - 折れ線グラフ
    - カラーチャート:
      - タイムライン帯グラフ（ストリップチャート）形式
      - 状態を色で表現し、タイムライン上で連続表示
      - ラベルを帯の中に常時表示
  - **グラフ種別共通**
    - **X軸タイプ切り替え**:
        - 時間（月次）: 月ごとの推移（範囲選択で日次へドリルダウン可能）
        - 時間（日次）: 日ごとの推移（期間指定可能）
        - 使用回数: 0〜1000000の使用回数ベース
    - **Brush & Zoom**:
        - メインチャートの上に概要チャート（全期間表示）
        - マウスホイールでズーム、ドラッグでパン操作
        - ズームリセットボタン、現在の表示期間を表示
    - **アノテーション**:
        - イベント情報（FW更新、部品交換、メンテナンス、エラー発生）を縦線または散布図で表示
        - 使用回数軸の場合も推定使用回数に基づいて位置を特定し表示
        - チェックボックスでアノテーション表示/非表示切替
    - **詳細データ表示機能**:
      - **ページネーション**: デフォルト20件/ページ、10/20/50/100件から選択可
      - **ページジャンプ**: ページ番号を入力して直接移動
      - **ソート機能**: 機番・値のヘッダークリックで昇順/降順切替
      - **CSVダウンロード**: 詳細データをCSVファイルとしてエクスポート
  - **折れ線グラフのみ**
    - **多変量表示（2軸対応）**:
        - 最大2変数まで追加可能（左軸・右軸各1つ）
        - 変数ごとにカテゴリー・特性値ID・集計方法を選択
        - 左軸（系列1）と右軸（系列2）でスケールの異なるデータを重ねて比較可能
        - 系列2を追加してもレイアウトが崩れない2段構成UIを採用
        - 凡例による表示ON/OFF切り替え
- **URL状態管理**:
  - 選択中の期間・フィルタ条件・表示変数をURLパラメータに自動反映
  - URLパラメータ例: `s1_cat`, `s1_char`, `s2_vis`, `s2_cat` 等で各系列の状態を保持
  - 「URLコピー」ボタンで分析状態を即座に共有可能
  - URLアクセス時に自動でグラフ表示および状態復元
- **フィルタプリセット保存**: 条件をプリセットとして保存し、ワンクリックで呼び出し可能（LocalStorage）

### 5. 不具合傾向表示
- **レイアウト**: 上段に時系列チャート、中段に分布比較、下段に詳細リストの積み上げ構成
- **検索条件**: 
  - 機種シリーズ
  - 機種番号（複数選択可能）
  - 不具合分類
  - 不具合コード
  - 対象期間
- **チャート**:
  - **不具合発生推移**: 時系列の発生件数推移
  - **製造月別分布比較**: 
    - 棒グラフ: 「不具合発生機番」と「対象機種全体」の製造月ごとの台数分布（2軸表示）
    - 折れ線グラフ: 不具合発生率（%）を表示
    - 不具合が特定の製造月に偏っているか（ロット不良の可能性）を視覚的に判断可能にする
- **結果表示**: 
  - 該当機番一覧をテーブル表示
  - ページネーション対応（デフォルト20件/ページ、20/50/100件から選択可）
  - 検索結果件数バッジ表示
- **機番選択・操作**:
  - 全選択チェックボックス
  - 機番コピーボタン
- **画面遷移**: テーブル行クリック等はなし（現状はリスト表示のみ）
- **UX機能**:
  - 検索中のローディング表示
  - エラー発生時のアラート表示


---

### 6. パトロール結果一覧表示
- **目的**: 定期的なパトロール（監視ロジックによる自動検出）の結果を確認する。
- **検索条件**:
  - 監視ランク（A, B, C等）
  - 機種シリーズ
  - 機種番号（複数選択可能）
  - 不具合分類
  - 不具合コード
  - パトロール対象日（From-To）
  - 検出ロジック内容
  - アラートフラグ（あり/なし）
- **結果表示**:
  - テーブル表示（カラム：ランク、シリーズ、機種、機番、不具合分類、コード、対象日、件数、ロジック、アラート）
  - **ページネーション**: デフォルト30件/ページ（30/50/100件から選択可）
  - **ソート**: 各カラムで可能
- **画面遷移**:
  - 行をクリックすると、該当条件（シリーズ、機種、不具合内容、期間）を引き継いで「不具合傾向表示」へ遷移


---

### 7. データカタログ
- **目的**: アプリケーションで使用しているデータの構造と内容を確認する。
- **表示内容**:
  - データエンティティをカード形式で表示
  - 各カードにはテーブル名、カラム定義、マスターデータ例を表示
- **エンティティ一覧**:
  - **Machine（機番属性）**: 機番、シリーズ、機種、製造月、FWバージョン等
  - **Event（イベント情報）**: イベント種類、カテゴリ、コード等
  - **CharacteristicValue（特性値）**: カテゴリ、特性値ID、量的/質的変数
  - **PatrolResult（パトロール結果）**: ランク、ロジック、アラート等

---

## データベース設計
